# Create a dry.py program and check out the complete list of built-in functions:

#     Find 4 built-in functions that we have used previously in the course.
#     Pick 1 built-in function that we have not seen before.

# Use each of these once in the program.

# For the new function, try to read the documentation (😵‍💫) or Google it (👍)!

# Add a comment for each built-in function to explain how they work.

nome = input('Qual é o seu nome? ') #Lê um input de usuário, converte em string e armazena na variável declarada.
print('Que nome lindo 😳') #Argumentos são convertidos para string e mostrados na tela.

lista_nomes = ['Jacqueline']

for i in range(len(lista_nomes)): #Limita a quantidade de vezes que um loop é repetido. // Conta a quantidade de elementos que tem dentro de uma lista.
    lista_nomes.append(nome) #Insere informação dentro da lista declarada anteriormente.
    print(lista_nomes)